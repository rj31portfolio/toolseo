<div class="card stack"><h3>Trial & scheduling</h3><form data-api="admin/scheduling"><div class="form-grid"><?php field('trial_days','Trial days (0 disables trials)','number',setting('trial_days',0));field('trial_plan_id','Trial plan ID','number',setting('trial_plan_id',2));field('grace_days','Subscription grace days','number',setting('grace_days',0));field('ranking_frequency_hours','Ranking refresh interval (hours)','number',setting('ranking_frequency_hours',24));?></div><button class="button">Save scheduling settings</button></form></div>
<?php if($user['role']==='super_admin'):?><div class="card stack"><h3>Configure server providers</h3><p class="small">Values are written only to protected server configuration. For Gemini, use endpoint https://generativelanguage.googleapis.com/v1beta and model gemini-flash-latest. Leave a secret blank to preserve it. Use the explicit clear checkbox to remove credentials.</p><form data-api="admin/provider"><?php select_field('provider','Provider',['ai'=>'AI (Gemini / chat-compatible endpoint)','leads'=>'Business lead service','ranking'=>'Google rankings (data service / gateway)','keyword'=>'Keyword gateway','backlink'=>'Backlink gateway','smtp'=>'SMTP','razorpay'=>'Razorpay']);?><div class="form-grid"><?php field('endpoint','Endpoint URL / SMTP hostname','text','',false);field('key','API key / SMTP username','password','',false);field('secret','API secret / SMTP password','password','',false);field('extra','AI model / webhook secret / SMTP sender','text','',false);field('port','SMTP port','number','587',false);?></div><label class="check"><input type="checkbox" name="clear"> Clear this provider’s configuration</label><button class="button">Save private configuration</button></form></div><?php endif;?>
