document.querySelectorAll('[data-extra-checkout]').forEach(form=>form.addEventListener('submit',async event=>{event.preventDefault();const button=form.querySelector('button');button.disabled=true;try{const r=await api('extra-checkout',new FormData(form));if(typeof Razorpay==='undefined')throw new Error('Checkout failed to load.');new Razorpay({...r.data,handler:()=>{toast('Payment submitted. Waiting for server verification.');setTimeout(()=>location.reload(),4000);},theme:{color:'#ff7a00'}}).open();}catch(error){toast(error.message,true);}finally{button.disabled=false;}}));
