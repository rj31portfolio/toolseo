document.getElementById('checkout-form')?.addEventListener('submit',async event=>{event.preventDefault();const button=event.target.querySelector('button');button.disabled=true;try{const r=await api('checkout',new FormData(event.target));if(typeof Razorpay==='undefined')throw new Error('Checkout could not load. Check your connection.');const checkout=new Razorpay({...r.data,description:'One month of SEO AutoPilot',handler:()=>{toast('Payment submitted. Your plan activates after server verification.');setTimeout(()=>location.reload(),4000);},theme:{color:'#ff7a00'}});checkout.open();}catch(error){toast(error.message,true);}finally{button.disabled=false;}});
